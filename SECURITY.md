# 安全说明

## 当前边界

这是开发预览版，未作完整安全审计。模型 API 与管理入口面向同一 Windows 用户的回环环境；管理网页免密码进入，不是远程多人控制面板。不要将管理端口直接暴露到局域网或互联网。

上游 key 使用 Windows 凭据管理器；设备管理使用每设备 Protocomm Security 2。发布包不包含配对材料。设备转发 TLS 字节流，主机校验上游证书。默认没有写入 Secure Boot / Flash Encryption eFuse，镜像散列不等于发布者签名。

## 报告问题

项目发布到代码托管平台后，如仓库启用了私密漏洞报告，请使用该入口。尚未配置的邮箱或私密渠道不在此虚构列出。缺少私密通道时，可先提交不含利用细节的联系请求；不要在公开 issue 中发布凭据、可利用细节或用户数据。

安全报告至少包含受影响版本、前置条件、影响范围和最小脱敏复现。日志中删除 API key、本地 token、配对密码、Wi-Fi 密码、cookie 和授权头。泄露的实际凭据需要由持有人撤销或更换，不能仅靠删除仓库中的文本解决。
