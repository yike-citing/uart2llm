# 贡献指南

欢迎围绕可复现问题、测试、文档和平台适配提交贡献。当前主要目标是补齐 Windows / ESP32-S3 的稳定性与离线验收，再扩展平台和传输方式。

## 本地开发

从[构建指南](docs/BUILDING.md)开始。用合成数据和独立测试目录，设备测试使用自己的开发板及配对材料。不要使用真实业务数据作为提交的 fixture。

提交前运行与改动相关的 Go、前端、Python 或固件检查。纯文档变更运行 `python scripts/check-source.py` 检查本地链接和常见敏感材料。该扫描不能代替人工检查。

## 问题报告

说明软件/固件版本、Windows 版本、GPIO UART 或 USB 连接方式、预期行为、复现步骤和脱敏错误码。说明测试是否物理断开电脑其他出口。不要直接上传完整配置、配对文件、系统凭据库、cookie、crash dump 或模型请求正文。

## 修改约定

- 保持网关无电脑出口回退、不自动重放请求、缓冲有界的约束。
- 管理配置通过注册表与事务机制实现，界面不直接编辑内部状态。
- 并发、取消、配置回滚和安全握手变更必须覆盖失败路径。
- 不因上游缺少资源而伪造成功响应；不把未测试项写成已支持。
- PR 描述具体问题、行为变化、验证方式和剩余限制；第三方代码保留来源和许可。

发布版本前还需真实硬件、掉电与持续负载验收。源码检查通过不等于成品级验收已完成。
