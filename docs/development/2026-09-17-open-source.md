# 2026-09-17 开源副本整理记录

## 结构与发布材料

- 保留原有源码模块与相对构建路径，建立独立源码副本；未移动正在使用的开发工程或运行配置。
- 排除本机设备配对、账户凭据、会话、原始测试报告、历史镜像、大型工具链与构建缓存。
- 编写项目首页、文档索引、首次使用、构建指南、架构、验证状态、贡献、安全及第三方说明。
- 增加离线 HTML/CSS 项目介绍页，包含架构示意、能力、操作步骤及未通过项；无外部字体、图片或追踪脚本。
- 原创代码采用 MIT，第三方通知及 esptool 对应源码独立保留。
- 固件构建辅助脚本使用已激活的 IDF 环境，移除开发者安装路径假设；Windows 打包不再依赖特定日期的私有实板报告。
- 增加源码清单、链接与常见敏感材料检查、带文件散列的源码 ZIP 生成器。

## 本次验证

| 检查 | 结果 |
|---|---|
| Web 锁定依赖安装、TypeScript、生产构建 | 通过 |
| Web 单元测试 | 72/72 通过 |
| Go Windows 后台编译 | 通过 |
| Go Windows 测试 | 多数包通过；link-recovery、config、credentials 三组被本机应用控制策略阻止执行，未计为通过 |
| Go 1.27.1 `vet` | 报告现有 `internal/tray/tray_windows.go` 的 GlobalLock 返回值转指针用法；未为源码整理任务改动运行代码，保留待审查 |
| Python 回归 | 安装配对生成器依赖后 12 项通过；1 项历史本机证据检查因未包含原始证据而跳过 |
| Harness 调度/安装器 | 13/13 通过；检查环境安装了匹配版本的 Harness |
| 项目介绍页 | 本地浏览器完成桌面和窄屏检查；修正窄屏标题孤字换行，无横向溢出；公共静态文件，无登录要求 |

首次 Python 检查缺少 `esp-idf-nvs-partition-gen`，已安装到局部测试依赖目录后重测，不修改全局 Python。没有更改 Windows 应用控制策略。源码整理不等于修复全部既有缺陷，也没有把原先未通过的 Harness 搜索、网页读取与长期稳定性验收更新为通过。

本次没有重新烧录设备、创建公开仓库、上传代码或发布安装包。最终源码归档附带逐文件 SHA-256；公开之前可再次运行 `scripts/check-source.py` 并复核许可与实际发布文件。

## 介绍页第二次修订

按产品定位精简首页与 README：面向互联网访问不便和内网开发者，强调保持 IDE/工具工作流、标准 OpenAI 兼容接口、后续 Responses API 计划、轻量 Agent 和离线安装。删除首页状态区块，将第二部分改为“完善的数据遥测与统计”。

网络行为表述为不创建系统网络接口、不安装虚拟网卡、不修改系统路由，不作“任何安全软件都不会报警”的保证。完整技术资料与既有测试结论保留在独立文档。此次仅调整公开文案；不变更产品能力或测试结论。页面和 README 的本地链接检查通过，已在浏览器核对新文案和布局，源码归档同步更新为 r2。
